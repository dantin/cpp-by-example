os: linux
dist: xenial

addons:
  apt:
    packages:
      - python3-pip

language: cpp

jobs:
  include:
  - stage: static check
    before_install:
      - sudo pip install cpplint
    script:
      - make check
